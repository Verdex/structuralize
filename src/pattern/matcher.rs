
use std::collections::HashMap;

use crate::data::*;

use super::data::*;

// TODO:  it would be nice if the vec was some sort of iter
pub fn pattern_match(data : Data, pattern : Pattern) -> Result<Vec<HashMap<String, Data>>, MatchError> {

    Ok(vec![])
}